# ai_assignment
repo for my ai assignment for mat311

This notebook is trying to look at the trends of fraudulant card transactions. We look at multiple variables including distance from home, distance from last transaction, chip used, pin used, etc. Through this notbook we hope to notice a trand that will allow us to correctly predict when a transaction is fraudulant or not. In this assingment, we used AI to help us generate code that will produce the results we are lookign for like the shape of the data and how many null values there are as well as displaying the data in a way that is easy to understand and interpret the results.

# Reflection
## What concrete benefits did you see by utilizing AI to help you make the notebook?

The biggest benefit that I found was when making the graphs. AI was able to make my graphs readable and aesthetically pleasing much better then I could. The AI bot that I used knew what were the best areguments to use that would make my charts and bar graphs the most readable. AI also labeled my graphs much better then I could as well as adjusting the size to be the most readable. The plot that AI produced looked much more aesthetically pleasing then it would have looked if I had created it. 

## What dangers do you see for soemone using AI to make this notebook without learning data science properly? 

While AI is able to make very good plots, sometimes those plots were not the best way to present this data. For example, when I asked AI to create a couple of plots looking at the distribution of transactions that used chip and how many were fraud and how many were not it created a histogram for me. This was not the best representation sicne there were only two values that the data could take on. The histogram was confusing and would be hard to interpret. If someone had not learned about data science and when to use certain graphs then they would just use the first graph that AI created for them rather then what would be the best graph to represent the data. 

## Give examples where you needed to verify the AI output for correctness. How did you know to verify those outputs?

For all of graphs that the AI generated I checked against what the correct output was supposed to be. The code that the AI bot generated didn't show its output, so I needed to confirm that this code would produce results that were identical to what was expected. I double checked almost all of the outputs that my AI generated code produced simply because I wanted to ensure my answers were correct. AI can be wrong or not give you specifically what you want. So, I doublechecked everything, but I would check my graphs multiple times to ensure that those results were correct. 
